-----------------------------------------------------------------------------------------------------------------------------------------
-- CONFIGURAÇÕES GERAIS
-----------------------------------------------------------------------------------------------------------------------------------------

Config = {}

-----------------------------------------------------------------------------------------------------------------------------------------
-- CONFIGURAÇÕES DO COMANDO
-----------------------------------------------------------------------------------------------------------------------------------------
Config.Command = 'fpsbooster' -- Comando para abrir a interface

-----------------------------------------------------------------------------------------------------------------------------------------
-- CONFIGURAÇÕES DE PERMISSÕES
-----------------------------------------------------------------------------------------------------------------------------------------
Config.UsePermissions = false -- Ativar sistema de permissões
Config.AllowedGroups = { -- Grupos com permissão (se UsePermissions = true)
    'admin',
    'mod',
    'superadmin'
}

-----------------------------------------------------------------------------------------------------------------------------------------
-- CONFIGURAÇÕES DE INTERFACE
-----------------------------------------------------------------------------------------------------------------------------------------

-- Idioma
Config.Language = 'pt-BR' -- Idioma da interface

-- Valores Padrão
Config.DefaultValues = { -- Valores padrão dos sliders
    shadows = 0,                        -- Sombras (0-100)
    textures = 0,                       -- Texturas (0-100)
    effects = 0,                        -- Efeitos (0-100)
    lighting = 0,                       -- Iluminação (0-100)
    distance = 0                        -- Distância (0-100)
}

-----------------------------------------------------------------------------------------------------------------------------------------
-- CONFIGURAÇÕES DE OTIMIZAÇÃO
-----------------------------------------------------------------------------------------------------------------------------------------

Config.OptimizationLevels = {
    shadows = {
        min = 0, -- Valor mínimo do slider
        max = 100, -- Valor máximo do slider
        step = 1 -- Incremento do slider
    },
    textures = {
        min = 0,
        max = 100,
        step = 1
    },
    effects = {
        min = 0,
        max = 100,
        step = 1
    },
    lighting = {
        min = 0,
        max = 100,
        step = 1
    },
    distance = {
        min = 0,
        max = 100,
        step = 1
    }
}

-----------------------------------------------------------------------------------------------------------------------------------------
-- CONFIGURAÇÕES DE SISTEMA
-----------------------------------------------------------------------------------------------------------------------------------------

-- Debug
Config.Debug = false -- Ativar mensagens de debug no console

-- Intervalos
Config.SaveInterval = 5000 -- Intervalo para salvar configurações (ms)

return Config 